{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="container" style="background: navajowhite;border: 0;margin-left: 10px;">
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ user.name }}</td>
            </tr>
        </tbody>
    </table>
     <p>
    <a class="btn btn-info w-25" href="{{ path('user_index') }}">back to list</a>
   <p>
   <p>
    <a class="btn btn-info w-25" href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
	<p>
   <p>
    {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
